# ==========================================
# 1. BỎ QUA RÁC BIÊN DỊCH CỦA KEILC (MDK-ARM)
# ==========================================
# FIX: Bỏ prefix "STM32_BareMetal_Test/" — .gitignore đã ở root rồi
Objects/
Listings/
DebugConfig/
*.o
*.d
*.crf
*.map
*.axf
*.htm
*.lnp
*.sct
*.dep
*.build_log.htm
*.iex
*.lst

# ==========================================
# 2. BỎ QUA FILE CẤU HÌNH CÁ NHÂN & DEBUG
# ==========================================
*.uvguix.*
# FIX: KHÔNG ignore *.uvoptx — chứa debug config cần thiết
# *.uvoptx   ← bỏ dòng này ra
*.scvd
JLinkLog.txt
*.bak
*.base@*

# FIX: KHÔNG ignore toàn bộ RTE/ — chứa startup files cần build
# RTE/   ← nguy hiểm, bỏ dòng này ra
# Chỉ ignore file backup do Keil sinh ra:
RTE/**/*.base@*

# ==========================================
# 3. BỎ QUA CẤU HÌNH CỦA EDITOR KHÁC
# ==========================================
# FIX: Không ignore toàn bộ .vscode/ — giữ lại settings cần thiết
# .vscode/   ← bỏ dòng này ra
.vscode/ipch/
.vscode/*.log

# KHÔNG ignore: .vscode/c_cpp_properties.json (IntelliSense include paths)
#               .vscode/launch.json (ST-Link debug config)
#               .vscode/settings.json

.codemap/
.gitnexus
.claude/
AGENTS.md
CLAUDE.md

# ==========================================
# 4. OS ARTIFACTS
# ==========================================
Thumbs.db
.DS_Store
desktop.ini